# Changelog

- 2026-06-08: Auto-apply DB migrations on deploy. Added a `vercel-build` script (`drizzle-kit migrate && next build`) so Vercel applies pending migrations (e.g. `0003_job_discovery_pipeline`) automatically at deploy time. Local `build` stays `next build` only. Makes the job-discovery tables provision without a manual `db:migrate` step.
- 2026-06-08: Security — harden outbound fetches against SSRF. New `src/lib/net/safe-fetch.ts` (`fetchExternalHtml`) validates every attacker-controlled URL the server fetches (JD scraping + discovery crawling): http/https-only scheme allowlist, no embedded credentials, literal private/reserved IP block (IPv4 incl. alt forms + IPv6 incl. mapped/ULA/link-local), DNS-resolution check (every resolved address must be public — defeats rebinding to 169.254.169.254 / RFC1918), no auto-follow redirects, 10s timeout, and a 2.5 MB response-size cap. Wired into `jd-scraper` and `run-discovery`. 11 new tests (total 60). Earlier black-box review confirmed access control is sound — every job/resume/fit/tailored data-layer call self-authenticates via `requireJobAccess`/`requireResumeAccess`.
- 2026-06-08: Fix sign-in hang on Vercel preview deploys — `auth-client` no longer hardcodes `NEXT_PUBLIC_BETTER_AUTH_URL`; defaults to `window.location.origin` so sign-in works on prod, every preview, and localhost without env config.
- 2026-05-27: Added job discovery and tailored-resume pipeline scaffolding: search profiles, curated sources, discovery runs/listings, resume-job fit checks, tailored resume copy persistence, print-ready PDF download route, dashboard/job/resume UI wiring, and focused tests for discovery, fit schema, and tailored title behavior.
- 2026-05-14: Initial scaffold.
- 2026-05-14: Database layer scaffold — Drizzle ORM + Neon HTTP driver, empty schema placeholder, drizzle-kit config & scripts (generate/migrate/push/studio), t3-env validation, `.env.local.example`.
- 2026-05-14: Better Auth wired with Drizzle adapter (email/password enabled). Generated `auth-schema.ts` with user/session/account/verification tables + relations. Auth route handler at `/api/auth/[...all]`. Initial migration `0000_amazing_terrax.sql`. DB client now lazy-initialized to keep `next build` working when `SKIP_ENV_VALIDATION=true`.
- 2026-05-14: shadcn/ui initialized (Neutral, CSS variables). Components installed: button, input, label, card. `cn` helper at `src/lib/utils.ts`.
- 2026-05-14: Auth UI — `/sign-up`, `/sign-in` (client forms, shadcn Card), `/dashboard` (server-gated by `auth.api.getSession`). Session-aware landing page. `SignOutButton` client component.
- 2026-05-14: Anthropic SDK + Helicone proxy scaffold. `src/lib/ai/anthropic.ts` exposes `getAnthropic()` (lazy), `src/lib/ai/models.ts` defines PLANNER (opus-4-7) / EXECUTOR (sonnet-4-6) / REVIEWER / CHEAP. Env adds `ANTHROPIC_API_KEY`, `HELICONE_API_KEY`.
- 2026-05-14: Phase 2 / Chunk 2 — v0.1 Drizzle schema. `src/lib/db/resume-schema.ts` adds resume, contact_info, experience, bullet, education, skill, project tables with text IDs (`crypto.randomUUID`), `timestamp({ withTimezone: true })`, cascade-on-delete FKs, FK indexes, and full relations. Migration `0001_square_riptide.sql`.
- 2026-05-14: Phase 2 / Chunk 3 — ResumeImporter agent. Vitest test infrastructure (vitest + vite-tsconfig-paths). Zod data contract at `src/lib/ai/resume-importer/schema.ts` (`ParsedResumeSchema`). Agent at `src/lib/ai/resume-importer/index.ts` uses Opus 4.7 + Anthropic tool-use with forced `tool_choice` for structured output; accepts text or PDF (Vercel-Blob-style URL) input; validates output via Zod. 6 passing tests (TDD: red→green) mock the Anthropic SDK.
- 2026-05-14: Phase 2 / Chunk 4 — end-to-end import flow (read-only). `createResumeForUser` orchestrator (`src/lib/resumes/create.ts`) handles text path direct + PDF path via Vercel Blob upload, calls `importResume`, persists. Repo layer (`src/lib/resumes/repo.ts`) inserts resume + contactInfo + experiences + bullets + educations + skills + projects with best-effort cleanup (neon-http has no transactions). Server action `createResumeFromImportAction` (`src/app/actions/resumes.ts`) handles auth, FormData, redirect. `ImportResumeButton` client component (shadcn Dialog + Tabs + Textarea, useFormStatus pending). Dashboard now lists resumes with empty state. Read-only `/resume/[id]` server component renders contact/experience/education/skills/projects sections with date formatting and skill grouping. New deps: `@vercel/blob`, shadcn dialog/tabs/textarea. Env adds `BLOB_READ_WRITE_TOKEN`. 3 new TDD tests for the orchestrator (text path, pdf path, error propagation) — total 9 passing.
- 2026-05-14: Phase 2 / Chunk 5 — inline editing with save-on-blur. Patch schemas (`src/lib/resumes/schemas.ts`) for all 7 editable entities, strict-mode Zod. Access helper (`src/lib/resumes/access.ts` + `owner.ts`) with `requireResumeAccess` doing auth + ownership in one call, throwing `UnauthorizedError` / `ForbiddenError`. Repo update functions with FK-scoped WHERE clauses (bullet update verifies experience belongs to resumeId via subquery; child updates verify resumeId match) — every update touches `resume.updatedAt`. 7 server actions in `src/app/actions/resumes.ts` (updateResume/ContactInfo/Experience/Bullet/Education/Skill/Project) — each ~6 lines: auth → Zod parse → repo. `EditableText` (single-line + multiline with auto-grow) and `EditableDate` client primitives. `ResumeEditor` client component renders the whole resume with editable cells. `/resume/[id]` server page now passes data to ResumeEditor. 4 new tests on access helper (unauthed, missing resume, foreign owner, success) — total 13 passing.
- 2026-05-14: Phase 2 / Chunk 6 — add/delete/reorder for list-shaped fields. Pure `validateOrderedIds(existing, proposed)` permutation guard (`src/lib/resumes/reorder.ts`) with 6 tests covering same-set, empty, length-mismatch (both directions), foreign-id injection, and duplicate-id smuggling. Repo: 5 add functions (`addBulletRow`/`addExperienceRow`/etc.) that compute next `sortOrder` via `count()`; 5 delete functions (FK-cascade for children); 5 reorder functions (`reorderBulletsRow` + a `reorderChildren` helper that union-types over the 4 resume-child tables — `experience`/`education`/`skill`/`project`). 15 new server actions following the established `requireResumeAccess` → repo pattern. `ResumeEditor` extended with hover-revealed `ItemControls` (lucide ChevronUp/Down + X) and `AddButton` per section; uses `router.refresh()` to sync server state. Total 19 tests passing.
- 2026-05-14: Phase 3 / Chunk 7 — JDScraperAgent (v0.2). New `job` + `application` tables in `src/lib/db/jobs-schema.ts` (text IDs, `timestamp({ withTimezone: true })`, FK indexes; cascade on user, cascade on job, set-null on resume). Migration `0002_open_spirit.sql` is additive only. `src/lib/ai/jd-scraper/` adds Zod `JobDescriptionSchema` and `scrapeJobDescription({ url })` — fetches URL with a UA header, passes raw HTML to Opus 4.7 with forced `tool_choice`, validates output via Zod. 5 TDD tests covering happy path, non-200 fetch (skips Claude), missing tool_use, Zod failure, and model/tool_choice contract. Total 24 tests passing. No UI yet — chunk is agent + schema only.
- 2026-05-14: Phase 3 / Chunk 8 — BulletTailorerAgent (v0.2). `src/lib/ai/bullet-tailorer/` adds Zod `TailoredBulletsSchema` (`{ bullets: Array<{ originalText: string|null, text: string, rationale: string }> }`) and `tailorBullets({ experience, jobDescription })` — calls Claude **Sonnet 4.6** (`MODELS.EXECUTOR`) with forced `tool_choice`, system prompt enforces truth-preserving 1:1 bullet rewrites with per-bullet rationale, output validated via Zod. 5 TDD tests cover happy-path bullet count, Sonnet 4.6 model contract, forced `tool_choice`, missing tool_use, and Zod failure. Total 29 tests passing. No persistence yet — accept/reject preview UI is a deferred follow-up.
- 2026-05-14: Phase 3 / Chunk 9 — minimal v0.2 UI wiring (preview only, no accept/reject persistence). New `src/lib/jobs/` module: `owner.ts` + `access.ts` (`requireJobAccess`), `repo.ts` (`insertJob` + `getJobForUser`), `create.ts` (`createJobForUser` orchestrator), `tailor-repo.ts` (`loadJobForTailoring` + `loadResumeForTailoring`), `tailor.ts` (`tailorResumeForJob` — auths both ids, fetches both rows, calls `tailorBullets` per experience in parallel, returns `{ job, experiences: [{ experienceId, company, role, before, tailored }] }`). Shared `UnauthorizedError` / `ForbiddenError` lifted to `src/lib/access-errors.ts` so jobs and resumes can both depend on them without a DB-import cycle. Server actions `createJobFromUrlAction` (FormData → scrape → persist → redirect) and `tailorResumeForJobAction` (preview-only delegate). UI: `/job/new` (single URL input, shadcn Card + Input + Button) and `/job/[id]` (server-rendered JD details + new `JobTailorPanel` client component — pick a resume → preview before/after diff per experience with rationale shown via native `title=` and a CSS group-hover tooltip). Dashboard adds a parallel "Jobs" section with `+ Add job` CTA. **TDD on access, create orchestrator, tailor service, and the createJobFromUrlAction server action** — 13 new tests, total 42 passing. Vercel deploy `dcc7ed1` green.
- 2026-05-15: Phase 3 / Chunk 10 — switch resume PDFs to private Blob access (production fix). Prod was throwing `Vercel Blob: Cannot use public access on a private store` on every PDF import because the `resume-pdfs` store is intentionally private and `src/lib/resumes/create.ts` was passing `access: 'public'` to `put()`. Fix flips to `access: 'private'` and rewires `src/lib/ai/resume-importer/index.ts` to call `get(pdfUrl, { access: 'private' })` from the `@vercel/blob` SDK to stream bytes server-side, then sends them to Claude as a base64 document source (no URL leaks to Claude). Chose this over signed-URL approach because the SDK has no presigned-URL primitive for private blobs (`getDownloadUrl()` only appends `?download`) — option (a) is also cleaner to test (one SDK mock). Updated `resume-importer.test.ts` to mock `@vercel/blob` and assert the message content is `{ type: 'base64', media_type: 'application/pdf', data: <b64> }`; added a missing-blob test. `createResumeForUser.test.ts` now asserts `access: 'private'`. 1 new test, total 43 passing; typecheck, lint, build all green. Vercel deploy `3d875cc` green. ADR-013 (resume PDFs use private Blob access) added to bjc-portfolio.
